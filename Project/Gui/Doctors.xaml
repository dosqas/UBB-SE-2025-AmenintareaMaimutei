<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Project.Gui.Doctors"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Project.Gui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:models="using:Project.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <TextBlock Text="Doctor's List" HorizontalAlignment="Center" Padding="30"/>
        <ListView ItemsSource="Doctor">
            <ListView.ItemTemplate>

                <DataTemplate x:DataType="models:Doctor">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{x:Bind DoctorID}"></TextBlock>
                        <TextBlock Text="{x:Bind DoctorID}"></TextBlock>

                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
            
        </ListView>
    </Grid>
</Page>
