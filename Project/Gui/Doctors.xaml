<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Project.Gui.DoctorsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Project.Gui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:models="using:Project.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20">
        <StackPanel>
            <TextBlock Text="Doctor's List" HorizontalAlignment="Center" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" />
            <ListView ItemsSource="{x:Bind Doctors, Mode=OneWay}" BorderThickness="1" BorderBrush="Gray">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:Doctor">
                        <Border BorderBrush="LightGray" BorderThickness="0,0,0,1" Padding="10">
                            <Grid ColumnDefinitions="50, 1*, 1*, 1*, 1*, 1*, 1*, 100">

                                <TextBlock Text="{x:Bind DoctorID}" Grid.Column="1" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{x:Bind UserID}" Grid.Column="2" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{x:Bind DepartmentID}" Grid.Column="3" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{x:Bind Experience}" Grid.Column="4" HorizontalAlignment="Center"/>
                                <TextBlock Text="{x:Bind Rating}" Grid.Column="5" HorizontalAlignment="Center"/>
                                <TextBlock Text="{x:Bind LicenseNumber}" Grid.Column="6"/>

                                <Button Content="More Info" Click="MoreInfoClick" Tag="{x:Bind}" Grid.Column="7"
                                    Background="DodgerBlue" Foreground="White" Padding="5,2" FontSize="14"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>

</Page>
